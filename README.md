# BESS Streamlit Dashboard

This is a JSON-driven Streamlit version of the BESS System Dashboard.

## Runtime behavior

The app does not ask the user to upload Excel or JSON at runtime.

It reads:

```text
data/bess_dataset.json